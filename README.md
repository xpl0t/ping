## Ubuntu on network stereoids

This image is based on ubuntu and has the networking tools...

* ping
* curl
* wget
* traceroute

....already installed, so you don't have to. 😉

### Usage example

`docker run --rm -it xpl0t/ping`
